You are an expert Principal FinTech Engineer specializing in Quantitative Trading Systems, FastAPI (Python), React (TypeScript), and MongoDB. 

I need you to build an enterprise-grade, high-performance Stock Market Analytics Platform. The system must mirror TradingView Premium's UI/UX, feature bulk algorithmic stock scanning, auto-detect Head & Shoulders, moving averages, and candlestick patterns, and deliver an actionable predictive trade report (Entry, Stop Loss, Target).

### 1. RATE-LIMIT RESILIENT ARCHITECTURE (Backend)
To scan thousands of stocks without getting banned or rate-limited by Yahoo Finance/AlphaVantage, implement a background worker queue with asynchronous batch processing:
- Use an asynchronous chunking execution system (`asyncio.gather` with semaphores or Celery) that enforces a configurable delay (e.g., 0.5 seconds between batches) to guarantee rate-limit safety.
- Utilize a fallback proxy rotation interface and implement automatic jittered Exponential Backoff (`tenacity` library) if a 429 Rate Limit error is detected.

### 2. QUANTITATIVE ANALYSIS ALGORITHM ENGINE (`analyzer.py`)
Create a robust mathematical engine to process stock data frames:
- **Candlestick Core:** Use `pandas_ta` or a vectorized native matrix map to detect all major patterns (Hammer, Engulfing, Shooting Star, Doji, Morning/Evening Star).
- **Moving Averages:** Compute 20 EMA, 50 EMA, and 200 EMA to track dynamic Areas of Interest (AOI).
- **Head & Shoulders (H&S) Geometric Engine:** 1. Smooth the close data using a Savitzky-Golay filter to eliminate micro-noise.
  2. Implement `scipy.signal.find_peaks` to isolate local highs and lows.
  3. Search the peak arrays for sequences matching: Peak 1 (Left Shoulder), Peak 2 (Head, where H > P1), Peak 3 (Right Shoulder, where P3 < H and absolute difference between P3 and P1 is within a 10% threshold).
  4. Construct a linear equation for the "Neckline" connecting the intervening valleys.
- **Predictive Trade Report Engine:** If a breakout occurs (Price crosses the Neckline or an EMA bounce coincides with a Bullish candlestick pattern):
  - **Entry Price:** Current close price.
  - **Stop Loss (SL):** Calculated using 1.5x Average True Range (ATR) below the entry or the lowest local structural support point.
  - **Take Profit (TP):** Project a 1:2 or 1:3 Risk-to-Reward ratio, or project the absolute depth of the H&S pattern vertically from the neckline break.
  - **Expected Profitability Report:** Save a calculated risk assessment configuration for the "upcoming timeframe" to MongoDB.

### 3. FASTAPI ENDPOINTS & MONGODB STORAGE
- `POST /api/scanner/run`: Triggers a non-blocking background task scanning an array of tickers (e.g., S&P 500).
- `GET /api/scanner/report`: Fetches the calculated trade dashboard report, sorted by highest winning probability, including explicit Entry, SL, and TP objects.
- MongoDB Schemas (using Motor): Model `TickerAnalysis` to store the historical OHLCV data, patterns array, and computed drawing vectors, alongside a `Watchlist` schema.

### 4. TRADINGVIEW-GRADE FRONTEND (React + TS + Tailwind)
- **UI Styling:** Implementation of a pure TradingView Dark Theme (`bg-[#131722]`, `text-[#d1d4dc]`, custom crosshairs, and a grid system).
- **Charting Engine:** Integrate `lightweight-charts` (the official open-source high-performance canvas engine by TradingView).
- **Overlays & Markers:** - Render candle bars, EMA lines, and volume histograms cleanly.
  - Plot markers (`shape: 'arrowUp'` or `'arrowDown'`) at precise coordinates for candlestick entry signals.
  - Programmatically overlay calculated polygon vectors illustrating the Left Shoulder, Head, Right Shoulder, and Neckline coordinates.
- **Bulk Scan & Signal Matrix Dashboard:** A comprehensive tabular layout showing a real-time summary of the bulk analysis: Ticker | Identified Pattern | Bias (Buy/Sell) | Entry | Stop Loss | Target | Win Conviction %.

Please generate clean, fully integrated, modular, production-ready code blocks across these layers. Ensure explicit mathematical calculations for pattern recognition are fleshed out without using hand-waving or pseudocode comments.